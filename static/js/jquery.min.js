$(document).ready(function(){
    arr = ['Sugarcane','Rice','Maize','wheat','Barley','pulses'];
    ls= [6000,10000,2000,3000,2500,1500]
    var Getdata = $.get('/display');
    Getdata.done(function(data){
        var n = data.pre;
        var per_arr = data.results;
    $("#btn1").click(function(){
      // console.log("Helloo");
      var quantity = $("#formGroupExampleInput").val();
      var answer = $('input[name="gridRadios1"]:checked').val();
    //   alert(answer);
    var i;
    if (n == $('input[name="gridRadios1"]:checked').val()){
        alert("Caution you are using same crop !!"+' "'+n+'" ');
    }
    if (quantity>0){
            if (arr[0] == answer){
                quantity = quantity * 60;
                i=0
                $('.modal-body ').html("The estimated amount of crop you can grow is "+quantity+"(in tones)" +" of <b>"+  answer+'</b>');
            }
            else if(answer == arr[1]){
                quantity = quantity * 3;
                i=1
                $('.modal-body').html("The estimated amount of crop you can grow is "+quantity+"(in tones)" +" of <b>"+ answer+'</b>');

            }
            else if(answer == arr[2]){
                quantity = quantity*3;
                i=2
                $('.modal-body').html("The estimated amount of crop you can grow is "+quantity+"(in tones)" +" of <b>"+ answer+'</b>');
            }
            else if (answer == arr[3]){
                i=3
                quantity = quantity * 40;
                $('.modal-body').html("The estimated amount of crop you can grow is "+quantity+"(in tones)" +" of <b>"+ answer+'</b>');
            }
            else if(answer == arr[4]){
                i=4
                quantity = quantity * 6;
                $('.modal-body').html("The estimated amount of crop you can grow is "+quantity+"(in tones)" +" of <b>"+ answer+'</b>');
            }
            else if(answer == arr[5]){
                i=5
                quantity = quantity *3;
                $('.modal-body').html("The estimated amount of crop you can grow is "+quantity+"(in tones)" +" of <b>"+ answer+'</b>');
            }
            
            
    }
    else{
        $('.modal-body').html("Please enter the Acres !!");
    }
    var x = (quantity / ls[i]) * 100;
    // console.log(per_arr[1]);
    console.log(i)
    var li = per_arr[i] + x;
    console.log(li,quantity);
    if (li >= 100){
        alert("Caution Exceeded limit !!");
    }
    
    
    
    //   console.log(ans
    });
    });

    var Getdata = $.get('/display');
    Getdata.done(function(data){
        var n = data.pre;
   
    $("#btn2").click(function(){
       
        var res = $('input[name="gridRadios3"]:checked').val();
        console.log(n);
        console.log(quantity);
        if (res){
            if (n == $('input[name="gridRadios1"]:checked').val()){
                alert("Caution you are using same crop !!"+' "'+n+'" ');
            }
        }
        
        
    });
})



    

  });